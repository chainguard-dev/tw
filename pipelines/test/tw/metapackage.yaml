name: Meta Package Check

needs:
  packages:
    - package-type-check

pipeline:
  - name: Check if the package is a Meta Package
    runs: |
      pkg="${{context.name}}"

      package-type-check meta "$pkg"
