name: Static Package Check

needs:
  packages:
    - package-type-check

pipeline:
  - name: Check if the package is a Static Package
    runs: |
      pkg="${{context.name}}"

      package-type-check static "$pkg"
